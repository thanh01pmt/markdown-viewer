// src/lib/interactive-quiz-kit/types/common.ts

// This type alias is used to indicate that a string is expected to contain Markdown content.
// It improves readability and intent in type definitions.
export type MarkdownString = string;

// This type alias will be used for content that can be either raw Markdown or rich text HTML.
// For now, we'll keep it simple.
export type RichContentString = string;