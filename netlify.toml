[build]
  command = "node scripts/sync-content.mjs $CONTENT_SOURCE $PROGRAM_ID && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22.12.0"
  PROGRAM_ID = "pathway-aiot"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
