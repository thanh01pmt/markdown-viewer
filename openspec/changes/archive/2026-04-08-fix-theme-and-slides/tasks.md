# Tasks: Fix Theme and Slides

## 1. Setup & Layout Fixes

- [x] 1.1 Add `class="dark"` to `MainLayout.astro`
- [x] 1.2 Fix `className` in `index.astro`

## 2. Typography & Contrast

- [x] 2.1 Update text colors in `ModernMarkdown.tsx` for dark mode
- [x] 2.2 Reduce headings and body text size in `SlideRenderer.tsx`
- [x] 2.3 Adjust `prose` settings in `SlideRenderer.tsx`
- [x] 2.4 Use `ModernMarkdown` in `SlideRenderer.tsx` for proper slide parsing

## 3. Verification

- [x] 3.1 Verify contrast in lesson view (via code review & user screenshots)
- [x] 3.2 Verify slide scaling (fixed raw text/frontmatter issue)
- [x] 3.3 Verify build
